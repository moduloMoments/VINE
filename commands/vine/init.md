---
name: vine:init
description: "Scaffold .vine/hooks/ with project-specific templates based on repo discovery"
argument-hint: ""
allowed-tools:
  - Read
  - Glob
  - Grep
  - Write
  - Bash
  - Agent
  - AskUserQuestion
---

# vine:init — Project Setup

Initialize VINE for this repository by discovering available tools, agents, conventions, and
team patterns, then scaffolding `.vine/hooks/` with project-specific templates.

## What This Does

1. Discovers what's available in the repo (tools, agents, skills, commands, CI, test patterns)
2. Asks the engineer about team context that can't be derived from code
3. Generates `.vine/hooks/shared.md` and per-phase hook files pre-filled with relevant context
4. Optionally adds `.vine/` to `.gitignore`
5. Introduces the engineer profile (builds organically through vine:verify)

## Step 1: Discover Repo Capabilities

Scan the project for available tooling. Check each of these and catalog what you find:

**Claude configuration:**
- `.claude/commands/` — available slash commands (these can be suggested in hook files)
- `.claude/skills/` — available skills/agents (these can be wired into specific phases)
- `.claude/settings.json` or `.claude/settings.local.json` — project-level settings
- `CLAUDE.md` — project conventions already documented

**CI/CD and testing:**
- CI config files (`.github/workflows/`, `Jenkinsfile`, `.circleci/`, etc.)
- Test runners and config (`jest.config.*`, `cypress.config.*`, `vitest.config.*`, etc.)
- Lint/format config (`.eslintrc*`, `.prettierrc*`, `biome.json`, etc.)
- Package scripts (`package.json` scripts section, `Makefile`, etc.)

**Project structure:**
- Monorepo indicators (`packages/`, `apps/`, `domains/`, workspace config)
- Key directories and their purposes
- Existing documentation (`docs/`, `*.md` files)

Present a summary of what you found to the engineer before generating anything.

## Step 2: Ask About Team Context

Use `AskUserQuestion` to gather context that can't be derived from code:

- Team/domain ownership patterns (who owns what areas)
- PR review conventions (required reviewers, review tools)
- Any external integrations to wire in (Jira, Linear, Slack, Confluence)
- Testing expectations (coverage thresholds, required test types)

Keep it to 1-2 rounds of questions max. Don't over-interrogate.

## Step 3: Generate Hook Files

Create `.vine/hooks/` with the following files:

### shared.md

This file is loaded by ALL VINE phases. It should contain:

```markdown
# VINE Project Hooks — [Project Name]
# Generated by /vine:init on [date]
# Edit this file to customize VINE behavior for this repo.

## Available Tools & Agents

[List discovered commands, skills, agents with brief descriptions of when to use each]

## Project Conventions

[Key conventions from CLAUDE.md or discovered patterns — testing, linting, naming, etc.]

## Team Context

[Ownership, review patterns, external integrations — from engineer's answers]

## CI/CD

[How to run tests, lint, build — commands the engineer or Claude should use]
```

### Per-phase files (verify.md, inquire.md, navigate.md, evolve.md, pair.md)

Only create per-phase files where there's something phase-specific to add. Don't create
empty or boilerplate-only files. Examples of phase-specific content:

**verify.md** — Areas to always explore, domain-specific questions to ask, key config
files to check

**inquire.md** — Architecture decision templates, preferred patterns, design review
checklists specific to this project

**navigate.md** — Agents to invoke after code changes (e.g., code reviewers, test
generators), test commands to run per domain, lint/format commands

**evolve.md** — PR creation workflow (e.g., `/pr-create` command), CI validation
commands (e.g., `/ci-fix`), issue tracker integration for follow-up items, commands
and agents to suggest wiring into hooks for next time

**pair.md** — Preferred test commands for quick changes, lint/format requirements,
commit message conventions for small fixes

## Step 4: Gitignore

Check if `.vine/` is already in `.gitignore` or the user's global gitignore. If neither,
add `.vine/` to the project's `.gitignore` automatically. VINE artifacts are workflow state,
not repo artifacts — they shouldn't be committed by default.

If the engineer explicitly wants to commit VINE artifacts (e.g., for team sharing), they
can `git add -f .vine/` selectively.

## Step 5: Introduce Engineer Profile

After generating hooks, briefly mention the engineer profile to the engineer:

> "VINE can track your familiarity with different areas of this codebase so it adjusts
> explanation depth — more detail in unfamiliar domains, more concise where you're confident.
> This builds organically: the first time you run `/vine:verify` in a new domain, you'll be
> asked to rate your familiarity. No setup needed now."

Do **not** ask any domain rating questions here. The profile seeds itself through vine:verify
as the engineer works in different domains. This step is purely informational.

If `.vine/PROFILE.md` already exists (e.g., from a previous init or manual creation), skip
this message entirely.

## Step 6: Upgrade Existing Projects

If `.vine/hooks/` already exists (from a previous `/vine:init` or manual setup), run in
**upgrade mode** instead of overwriting:

1. Read all existing hook files
2. Re-run repo discovery to find new tools, agents, commands, or conventions added since
   the last init
3. Present a diff of what's new vs what's already in the hooks using `AskUserQuestion`:
   - New tools/agents discovered that aren't in hooks yet
   - Existing hook entries that reference tools/agents no longer present
   - Convention changes detected
4. Merge accepted changes into existing hook files — don't overwrite custom content the
   engineer has added

This makes upgrading after installing new skills, agents, or commands a one-command operation.

## Output

```
---
✅ vine:init complete
📁 Created:
   - .vine/hooks/shared.md
   - .vine/hooks/verify.md (if applicable)
   - .vine/hooks/inquire.md (if applicable)
   - .vine/hooks/navigate.md (if applicable)
   - .vine/hooks/evolve.md (if applicable)
   - .vine/hooks/pair.md (if applicable)

📋 Next step: Run `/vine:verify` to start your first feature.
   Your project hooks will be loaded automatically.

👤 Your engineer profile (.vine/PROFILE.md) will build as you work —
   each new domain prompts a quick familiarity check during verify.

💡 Tip: As you complete VINE cycles, `/vine:evolve` will suggest
   updates to your hook files and profile based on what you learn.
---
```
