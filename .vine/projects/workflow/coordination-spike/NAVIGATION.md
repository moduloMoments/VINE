# Navigation Log: Coordination Spike (Cycle 0)
## Date: 2026-06-11

### Slice 1: Eligibility gate run (Q1) — Complete
- **Started**: 2026-06-11
- **Commit**: f8600fd (pre-rebase c3dfa6a; branch rebased onto main 287fd02 at navigate close)
- **Route**: interactive (shepherd session) <!-- throwaway scaffold (spike Q2/Q6): per-slice route attribution; keep/discard at evolve -->
- **Actor**: Rob + Claude (shepherd) <!-- throwaway scaffold (spike Q2/Q6): per-slice actor attribution; keep/discard at evolve -->
- **Approach taken**: Hand-rolled the four-leg eligibility predicate against #47 (consolidate AskUserQuestion constraints block) and recorded the routing decision. Per-leg verdicts (*mechanical* = checkable by tooling/grep today, *judgment* = human/Claude call, *input-absent* = the field the leg needs doesn't exist):
  - **Leg 1 — Global validation contract: input-absent.** No machine-readable contract exists anywhere (#54 proposes a fenced YAML block in shared.md; today validation commands are prose in `.vine/context/navigate.md`). The de facto contract for #47's scope was reconstructed by judgment: `.vine/scripts/run-tests.sh` (hook-script suite) **plus** `.vine/scripts/trellis-check.sh` green (fresh `.vine/.trellis-ok` in the actor's own checkout) — required because #47 edits `commands/vine/*.md` and `trellis-gate.sh` fails closed.
  - **Leg 2 — Slice ACs: input-absent (structured field), judgment (prose fallback).** #47 is an issue, not a SPEC slice — no `**Acceptance criteria**:` field exists for it. Its Suggestion section carries derivable testable conditions (constraints table → shared.md "Interaction Constraints" section; inline blocks → single-line reference; graceful degradation when shared.md absent, same convention as #48). A cold actor can act on the prose, but the gate had to read and infer — nothing mechanical can check the field.
  - **Leg 3 — Slice independence: judgment (with mechanical sub-inputs).** "Independent of in-flight work" has no artifact representation. Checked live: 0 open PRs (`gh pr list` — mechanical query), but the load-bearing check was judgment: #87 landed on main today (`e017fca`) modifying verify.md and inquire.md — two of #47's four target files, in different regions (phase-completion steps vs. AskUserQuestion blocks). A file-set intersection check between delegated scope and recently-landed/in-flight work is mechanizable in principle; nothing implements it. #46's overlap with the same files is parked by inquire decision (only #47 runs).
  - **Leg 4 — Bounded blast radius: mechanical (enumeration) + judgment (threshold).** Grep mechanically confirms the constraints block lives in exactly 4 command files (verify.md:76–79, inquire.md:103–106, navigate.md:231–233, optimize.md:268–271) plus shared.md as target — matching the issue's claimed file set. Whether 5 files is "bounded enough" has no defined threshold anywhere — judgment.
  - **Decision: PERMIT — route #47 headless (`claude -p`, fresh worktree).** Gate-derived constraint for slice 2's delegation prompt: the actor's worktree must branch from **current main (`e017fca`, post-#87)**, not from this shepherd branch's base — otherwise it re-edits pre-#87 text in verify.md/inquire.md (leg 3 finding).
  - **Q1 rollup**: 0 of 4 legs fully mechanical today. One leg has a mechanical core (blast-radius enumeration via grep), one has a mechanical sub-input (open-PR query); two are input-absent (validation contract, structured ACs for issue-shaped scope). Matches CONTEXT.md's lean: *checkable in principle, not with today's fields.*
- **Deviations from spec**: None.
- **Validation**: pass (with one recorded pre-existing failure) — vine-verification agent (slice mode) + shepherd re-verification. All 3 slice ACs verified; NAVIGATION.md structural contracts (heading literal, required fields) pass; PROJECT-MAP tables well-formed; markdownlint skipped (not configured, per overlay's "once configured"). `run-tests.sh`: 19/23 — **4 pre-existing fixture failures**, observed not fixed per spike stance: #86 (`ce84718`) added trellis-check Check 10 (repo-level cross-reference anchors, resolved against `CLAUDE_PROJECT_DIR`), but `run-tests.sh`'s temp-dir fixtures were never updated to stub the anchor files, so every "expected pass" trellis-check fixture now exits 1. The real-repo run is green (`trellis-check.sh` exits 0, 8 anchor pairs resolve) — shepherd verified directly. Pre-existing on main (`e017fca` didn't touch either script). **Slice 2 consequence**: the delegation prompt must declare this known-red baseline (run-tests.sh = 19 pass / 4 known-fail in the trellis-check fixture group) so the headless actor neither halts on it nor "fixes" it.
- **Decisions made**:
  - Branch renamed `claude/distracted-hopper-cdc7c2` → `feature/coordination-spike`; new repo convention added to shared.md that VINE project slugs and feature branch names should match: (decided by: engineer)
  - Route column scaffolded as a separate scope-level table in PROJECT-MAP.md (scope ≠ phase row — the delegated #47 scope isn't a row in the VINE Progress table): (decided by: claude)
- **Acceptance criteria**:
  - [x] Route decision recorded with all four per-leg verdicts — above
  - [x] De facto validation contract for #47's scope named explicitly (`run-tests.sh` + `trellis-check.sh`, commands/vine edits trip the closed-fail trellis-gate) — leg 1
  - [x] Route column scaffolded into PROJECT-MAP.md with the decision — Route table added, marked throwaway
- **Engineer feedback incorporated**: Free-climb slice — branch/convention steer at slice start; boundary review pending.
- **Learnings**:
  - Claude → Engineer: The gate's one genuinely useful product beyond the verdicts was a *constraint discovered mid-check*: leg 3 (independence) caught that #87 landed on main today touching two of #47's target files, which sets where the actor's worktree must branch from. An eligibility gate that only returns permit/deny would have dropped that — the gate's output may need to be a verdict *plus* constraints fed into the delegation prompt.
  - Engineer → Claude: Slug/branch alignment matters for traceability — spawned worktree sessions arrive on auto-named branches, and the rename-to-match step is currently manual.
  - Claude → Engineer (spike evidence, feeds Q2): The vine-verification agent's *findings* were accurate (4 fixture failures, pre-existing, not this slice) but its *root-cause narrative* was inverted — it claimed #86 removed Check 10; #86 added it. Shepherd re-verification (running `trellis-check.sh` directly) caught it in one command. A headless actor consuming agent reports has no shepherd to do that re-check — agent reports may need to be treated as findings-trustworthy, diagnosis-unverified.

### Slice 2: Headless delegation run (Q2) — Complete
- **Started**: 2026-06-11
- **Commit**: b20a1fe (pre-rebase ca19d79)
- **Route**: interactive (shepherd session; the *delegated* work runs headless — see Route table in PROJECT-MAP.md) <!-- throwaway scaffold (spike Q2/Q6) -->
- **Actor**: Rob + Claude (shepherd); delegated actor: claude (headless, Agent-tool subagent — 4th mechanism) <!-- throwaway scaffold (spike Q2/Q6); field originally said `claude -p` and went stale when the mechanism diverged mid-slice — staleness caught by the Q6 parse probe (Slice 4), kept here as evidence for the mechanism-token fix -->
- **Approach taken**: Composed the delegation prompt as a tracked throwaway (`delegation-prompt-47.md` in this feature dir — spike evidence for slices 3–4). It pre-decides every interactive navigate decision point from CONTEXT.md's list: feature pre-supplied (#47), approve-edits skipped, branch pre-verified (`feature/askuser-constraints` off `e017fca`, per slice 1's gate constraint), gearing collapsed to free-climb, always-continue, test-coverage policy pre-set (markdown-only, no tests), PR surfaced-never-acted; plus hard file allowlist, byte-exact heading contracts, Route/Actor scaffold fields, journal-before-commit ordering, trellis-green-before-commit, the known-red `run-tests.sh` baseline (slice 1 finding), default-and-record vs. stop-and-surface split, `DONE:`/`BLOCKED:` exit protocol, and post-commit `Decisions Taken Autonomously` + `Environment Observations` appends left uncommitted for the shepherd. Actor worktree created at `.claude/worktrees/headless-47`; fired via `claude -p` (v2.1.119) in background.
- **Deviations from spec**: Mechanism diverged from SPEC decision 2 (`claude -p`) — the run was delivered by the 4th mechanism (Agent-tool subagent) after three informative platform failures (auth, remote provisioning, scheduled-task permission deadlock), each recorded under Decisions made. SPEC.md decision 2 carries the matching addendum. The divergence is itself spike evidence (mechanism-portability finding).
- **Validation**: pass — actor returned `DONE: bfe5458`; shepherd verified every claim against the worktree rather than trusting the report (slice 1 lesson): exactly one commit on `feature/askuser-constraints` touching only allowed files (4 command files + shared.md + actor journal, 40 insertions/30 deletions); journal heading byte-exact (`— Complete` literal); all six entry fields present including scaffolded Route/Actor; three handoff sections present and uncommitted as instructed; fresh green trellis stamp in the actor's checkout (00:29, 11/11 + 8 anchor pairs); worktree `.vine/ACTIVE` deleted. Actor's run-tests baseline claim (19/4, all four known) consistent with its checkout's pre-#89 scripts.
- **Decisions made**:
  - Delegated artifact home `commands/askuser-constraints`, branch `feature/askuser-constraints` — matches the existing `commands/` domain and the new slug/branch convention: (decided by: claude)
  - Headless permissions via `--dangerously-skip-permissions` — no silent stalls; PreToolUse hooks are not permissions, so observing trellis-gate/journal-check under bypass is exactly the Q2 test; throwaway worktree bounds blast radius: (decided by: engineer)
  - Spend cap `--max-budget-usd 10`: (decided by: engineer)
  - Mechanism divergence after `claude -p` auth failure (observations 1–3): engineer first chose a remote cloud agent (observation 4: blocked on provisioning + undocumented API), then a **local scheduled-task session** — fresh autonomous session in the desktop app, ad-hoc one-time fire, against the same `headless-47` worktree. Preserves the Q2 legs the cloud variant would blur (local repo hooks, ACTIVE lifecycle, journal-check mtime, real worktree); diverges from spec's `claude -p` mechanism — recorded, not silent. Delegation prompt v3 stored at `~/.claude/scheduled-tasks/headless-47-askuser-constraints/SKILL.md`; v1 (local `claude -p`) and v2 (remote) kept in this feature dir as evidence: (decided by: engineer)
  - Mechanism #4 after the scheduled-task permission deadlock (observations 5, 7): **Agent-tool subagent** of the shepherd session — fresh context window given only the delegation prompt (cold in the context sense, not the session sense); permissions ride on the shepherd session; hook scoping resolves to the shepherd's `CLAUDE_PROJECT_DIR` while git work happens in the `headless-47` worktree, deliberately exercising the journal-check/trellis-gate cross-checkout behavior as an observation. Engineer declined to hand-provision the scheduled task's allowed-tools; chose completing the loop tonight over mechanism purity: (decided by: engineer)
- **Acceptance criteria**:
  - [x] Delegation prompt pre-decides every interactive navigate decision point — see prompt file
  - [x] Prompt forces worktree isolation and requires trellis-check.sh green before any commit
  - [x] Prompt instructs Route/Actor fields, Decisions Taken Autonomously block, exact literal contracts
  - [x] Run completes or fails informatively; outcome recorded — three informative failures (auth, provisioning, permission deadlock) then a verified success (`bfe5458` via Agent-tool subagent)
  - [x] Empirical observations recorded per Q2 leg — observations 1–8: hook firing under headless (commit not blocked; fired-and-passed vs. didn't-fire indistinguishable from outcomes — live evidence for the scoping tech debt), `CLAUDE_PROJECT_DIR` (unset in subagent env; actor set it explicitly for trellis-check), nested Agent spawning (NOT available to subagents — vine-verification cannot run inside a subagent actor; answered definitively), ACTIVE lifecycle (actor wrote and cleaned up its worktree copy correctly; the deadlocked scheduled-task session never reached it), journal-check mtime (did not block the actor's commit; firing undetermined)
- **Engineer feedback incorporated**: Permission and budget decisions made at the fire point; delegation prompt offered for review before firing.
- **Q2 empirical observations** (running log): <!-- throwaway scaffold (spike Q2) -->
  1. **First fire: informative failure, pre-flight.** `claude -p` (v2.1.119) exited 1 with a 401 authentication error before touching anything — worktree clean, no commits, ACTIVE never written, no journal. Failure shape was exactly what the spike wants from failure: loud, clean, attributable (vs. silent/ambiguous).
  2. **Auth from inside a Claude Code session is its own platform leg nobody listed.** None of the auth-override env vars (`ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, `CLAUDE_CODE_OAUTH_TOKEN`) are set in the shepherd's shell; unsetting the `CLAUDECODE` nested-session marker changed nothing (hypothesis falsified by probe). `claude auth status` reports loggedIn=true (claude.ai method, Max subscription) while the API returns 401 — local credential state and server-side validity disagree, and headless `-p` cannot run the interactive refresh. Fleet-delegation consequence: actor auth must be *verified server-side* (a cheap probe call) before composing/firing a delegation, not inferred from local login state.
  3. **Diagnosis resolved by bisect + docs research (claude-code-guide agent).** The engineer's own terminal probe also 401s → token invalid server-side, not a spawned-process Keychain-access issue. Documented fix: full `logout` → `login` cycle (re-login over a stale token doesn't take); known macOS sleep/wake bug (claude-code#60104) produces this exact symptom. **The sanctioned headless-auth mechanism for subscription users is `claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN` (1-year lifetime, inference-scoped)** — for a real fleet, actor auth should be a long-lived scoped token provisioned per actor, not inherited interactive login state. That's a durable Q2 finding independent of today's outage.
  4. **Remote-actor leg (engineer-initiated probe): blocked on provisioning + API opacity — itself a Q2 finding.** The shepherd session has a `RemoteTrigger` tool for claude.ai cloud-agent triggers using in-session OAuth (which works — sidesteps CLI auth entirely). Driving it failed informatively: `POST /v1/code/triggers` is an internal/undocumented surface (validation errors revealed `session_request.worker` as an unguessable discriminated union; `job_config.ccr` demands an `environment_id` from Managed-Agents provisioning the account doesn't have). Docs research: the *documented* remote path is routines created in the claude.ai web UI and fired by a human or per-routine bearer token. Fleet consequence: remote actors are real but gated on account-level provisioning (GitHub app + environment) and, today, human-in-the-loop or token-provisioned firing — a shepherd can't conjure a remote actor ad hoc. Prompt variant composed for it is kept as evidence (`delegation-prompt-47-remote.md`).
  5. **Scheduled-task actor: fired on time, stalled at first tool call on a permission prompt.** Task fired 23:15:00 exactly; the actor session exists in the app's regular session list (`isRunning: true`, cwd = the ROOT checkout, not the worktree — `CLAUDE_PROJECT_DIR`/hook scoping will resolve to root) but last activity was 23:15:06 with zero worktree writes 24 minutes in. Headless-in-app is not permissionless: first-run tool approvals block silently with no notification to the shepherd. The shepherd only detected the stall by polling session state — exactly the missing-escalation-channel problem (observation: a stalled actor and a dead actor are indistinguishable from artifacts alone; the session registry was the only tell). Approvals are stored on the task once granted, so this is a first-run cost.
  6. **Gate verdicts decay: leg 3's independence check was stale within hours.** Between the slice 1 gate run and the actor's first edit, #88 and #89 merged to main (#88 plausibly touching the same command files as #47; #89 fixing the run-tests fixtures — making the actor's known-red baseline instruction checkout-relative rather than repo-current). The eligibility predicate's "independent of in-flight work" leg is a point-in-time answer; cycle 1's gate (#54) needs either a freshness window or a re-check at execution start.
  7. **Actor permission provisioning is a human-authorization boundary, enforced in two places.** The stalled run's root cause: scheduled-task sessions get no pre-granted tool permissions and no UI surface to approve them at runtime (the session never appeared in any list the engineer could see — shepherd found it only via the session registry API). The shepherd's attempted fix — adding `allowed-tools` frontmatter to the task's SKILL.md — was blocked by the platform's permission classifier as a self-modification grant the human hadn't requested. Both halves point the same way: granting an autonomous actor its toolset is a provisioning-time human decision (matching the engineer's work pattern, where a human configures the GitHub Action's permissions once). A VINE delegation design can't treat actor permissions as something the shepherd conjures; the gate's PERMIT verdict and the actor's tool grant are separate authorities. **Sharpened on retry**: even an explicit in-conversation human authorization (engineer chose "Authorize the grant" via AskUserQuestion) did not clear the classifier — it rejected the approval as "the agent's own self-generated prompt." Delegated authority doesn't transfer through conversation; the human must exercise it at the provisioning surface itself (edit the file, or write a settings rule). Cycle-1 design input: the delegation contract should assume actor permissions are provisioned out-of-band by the human, never brokered by the shepherd.
- **Learnings**:
  - Engineer → Claude (reference architecture, feeds Q2/Q4/Q5 and cycle 1's #53/#54): The engineer's work runs this loop in production — a Jira ticket triggers a GitHub Action that runs the review agent; the agent leaves notes as Jira comments; new comments or CI events re-trigger a fresh run. Three transferable properties: (1) actor provisioning is owned by CI infrastructure, never conjured by a shepherd — consistent with observation 4's remote-actor finding; (2) the escalation channel is a durable artifact (the ticket thread), not a live session — **event-driven re-entry** is a third route shape between "no channel" (`claude -p`) and "live channel" (agent teams), and it makes stop-and-surface degrade gracefully because surfacing IS the channel; (3) each re-run re-orients cold from durable state (ticket + PR), which is a production-scale vote for the Q3/Q4 hypothesis that reviewer orientation must come from artifacts alone.
  - Engineer → Claude (boundary thesis, the lens for slice 4's Q2 consolidation): **"VINE is the map, handoff contracts, gates, and specialized overlay context for actors — not the mechanism."** Empirically confirmed by tonight's four mechanism attempts (`claude -p` → remote trigger → scheduled task → Agent subagent): the delegation prompt's envelope (workspace, transport, permissions) was rewritten three times; its core (gate verdict + constraints, file allowlist, ACs, validation contract, journal schema with literal-string contracts, autonomy split, handoff shape) survived every swap unmodified. The mechanism failures were all platform-territory failures (auth, provisioning, permission surfaces); none touched VINE's layer. Q2's consolidation should report mechanism-portability of the contract surface as the finding, not a mechanism recommendation.
  - Claude → Engineer: The actor's `Decisions Taken Autonomously` block came back in exactly the decision—default—confidence—rationale shape the prompt specified, and it works: the two medium-confidence entries (shared.md section placement; dropping per-file multiSelect examples in the constraint merge) are precisely where a reviewer should look first. Confidence-tagged decision logs appear to be a viable triage signal for the reviewer leg — slice 3 should test whether the cold reviewer independently gravitates to those two.
  - Claude → Engineer: The actor also self-reported a scope edge worth keeping as a pattern: it found a stray one-phrase constraint mention in evolve.md (outside its file allowlist), left it untouched, and flagged it in the handoff instead of fixing it — the allowlist + stop-and-surface combination produced exactly the right behavior at a boundary the prompt never anticipated.

### Slice 3: Reviewer leg (Q3–Q5) — Complete
- **Started**: 2026-06-12
- **Commit**: d2c4c88 (pre-rebase ef5d84a)
- **Route**: interactive (shepherd session); the *reviewer* ran headless (Agent-tool subagent, cold context) <!-- throwaway scaffold (spike Q2/Q6) -->
- **Actor**: Rob + Claude (shepherd); reviewer actor: claude (cold subagent, agentId ad2274823037d1e89) <!-- throwaway scaffold (spike Q2/Q6) -->
- **Approach taken**: Scaffolded `.vine/context/review.md` (throwaway-marked) as the reviewer role recipe — deliberately **minimal** (orientation order, authority boundary, five required output sections including a contemporaneous missing-context log) and deliberately **excluding** the tribal artifact-format contracts, so Q3 could measure whether their absence bites rather than pre-filling the gaps. Ran the cold reviewer as an Agent-tool subagent (mechanism chosen by engineer; same precedent as slice 2's 4th mechanism — cold in the context sense, not the session sense). Reviewer inputs: review.md + the headless-47 worktree (commit `bfe5458`, actor journal incl. uncommitted handoff sections) + `gh issue view 47`; hard exclusions enforced in the prompt: `references/STATE.md` (any copy), the spike feature dir, other worktrees. Reviewer's sources-consulted log confirms compliance. Shepherd then re-verified the reviewer's load-bearing claims against the issue comment, the delegation prompt's allowlist, and the pre-edit file (slice 1 lesson: findings-trustworthy, diagnosis-unverified — here both held).
- **Deviations from spec**: None. (Spec said "fresh session"; engineer chose the Agent-subagent mechanism at the gearing decision, consistent with slice 2's recorded mechanism divergence. Cold-discipline caveat recorded under Q4: the subagent auto-loads CLAUDE.md, and that mattered — see scoring.)
- **Validation**: pass — vine-verification agent (slice mode) on review.md + this entry + PROJECT-MAP.md; all 5 slice ACs verified; structural contracts (heading literal, required fields) pass; run-tests.sh untouched by this slice (no script changes; known-red baseline unchanged on this pre-#89 branch).
- **Reviewer outcome**: **request changes** on `bfe5458`. Findings: F1 (major, shepherd-verified true) — issue #47's comment requires `vine:init` to scaffold/upgrade-offer the new "Interaction Constraints" section; `commands/vine/init.md` was neither updated nor flagged, leaving user repos (whose shared.md comes from init's template) with four dangling references. F2 (low, verified: 8 bullets, actor counted the header line) — journal miscount for inquire.md. F3 (low) — two same-class residual constraint phrases (navigate.md:329–331, verify.md:244) flagged inconsistently vs. evolve.md's. F4 (advisory) — concurs with all three of the actor's own handoff flags. Reviewer independently re-ran trellis-check (11/11) and run-tests (19/4, identical set) rather than trusting the report.
- **F1 root cause (shepherd analysis, feeds Q1/cycle-1 gate design)**: the actor behaved correctly inside its envelope — the delegation prompt's exhaustive allowlist excluded init.md, and the prompt carried only half the issue comment (graceful fallback as an AC; the init upgrade-pass requirement dropped). The gate's leg-4 blast-radius enumeration was a grep for the constraints block (4 files) — a *requirement-implied* file that no grep finds fell through. The dropped requirement propagated silently through gate → prompt → actor → handoff and was caught only because review.md's orientation order mandates reading the originating scope directly, not the handoff's paraphrase. Gate consequence: blast-radius enumeration must include requirement-implied files (comments included), not just occurrence-greps.
- **Q3 scoring (rubric = CONTEXT.md "Documentation Gaps", item-by-item)**:
  1. Required-vs-optional NAVIGATION.md fields — *mild hit*: reviewer logged it (missing-context #6) and skipped format-compliance review; narrowed scope, didn't mislead.
  2. Slice-status literal-string contract — *no hit*: human-shaped review never needed it.
  3. Deviation-closure contract — *no hit*: issue-shaped scope, no SPEC to cross-reference; ground-truth diff-vs-issue comparison substituted (and caught F1, which a deviation-field reading would have missed — the journal said "Deviations: None").
  4. Two-AC-layer model — *no hit*: no SPEC; single AC layer (prompt's ACs copied into journal).
  5. PROJECT-MAP legend/lifecycle — *no hit*: actor created no PROJECT-MAP (created-by-verify-only lifecycle held); reviewer never missed it.
  6. Context-for-future-sessions vs Remaining Work authority — *absorbed*: the recipe + envelope told the reviewer post-entry sections are handoff-addressed.
  7. Committing Artifacts table — *mild hit, absorbed by envelope*: uncommitted handoff sections would read as incomplete work without the envelope saying they're addressed to the reviewer.
  8. `(decided by:)` convention — *no hit*: the actor's journal used the structured block; the informal convention never appeared.
  **Net: 0 hard blocks from the predicted list** — but **four unpredicted gaps bit, all delegation-route-shaped** (reviewer's missing-context log #1–#4): (A) overlay adoption-scope — is shared.md product or contributor-only? Load-bearing for F1's severity; reviewer inferred it from CLAUDE.md. (B) Validation known-red baseline has no durable home (lived only in the delegation prompt); reviewer substituted a structural argument + re-run. (C) The delegation envelope itself (allowlist, gate constraints) is not durable-visible — reviewer could not tell whether init.md was excluded deliberately. (D) shared.md section-ordering contract — accepted the actor's claim unverified. **Q3 verdict**: a cold reviewer CAN orient and produce a load-bearing review from artifacts alone; the gaps that bite are not verify's predicted artifact-format gaps (those are SPEC/feature-shaped; this scope was issue-shaped) but the *gate record and delegation envelope lacking durable, reviewer-visible homes*. CONTEXT.md's "highest-value single chunk = STATE.md's NAVIGATION schema" guess is overturned: the highest-value chunk is a durable gate-record + envelope.
- **Q4 scoring (role hypothesis)**: **holds, with one promotion needed.** Overlay stack (review.md + auto-loaded CLAUDE.md) + entry point (the recipe, delivered via a ~20-line envelope prompt) + handoff contracts sufficed for a full review with a true major finding and independent validation re-runs. State needed outside the triad: the delegation envelope/gate record (missing-context #2–#3) — promote it into the handoff contracts. Honesty caveat: this reviewer was cold in context but ran inside the shepherd's session (CLAUDE.md auto-loaded, and missing-context #1 was answered *from* CLAUDE.md) — a truly cold E3 reviewer in a repo without this CLAUDE.md would have lacked the adoption-scope fact and might have mis-rated F1's severity.
- **Q5 scoring (one handoff artifact vs two)**: **one per-feature handoff artifact suffices — the second thing isn't an artifact, it's the role recipe.** The confidence-tagged `Decisions Taken Autonomously` block served outbound record and inbound triage simultaneously: the reviewer independently engaged with exactly the two medium-confidence entries (examples drop → F4a, placement → F4b), confirming slice 2's prediction that confidence tags are a viable triage signal. The outbound-structured vs inbound-prose tension resolved in practice: structure served the reviewer; the prose orientation lives durably in review.md, not per-feature. Caveat that shapes #53: a handoff-only review would have missed F1 — the recipe's mandate to read the originating scope directly is load-bearing and must stay part of the inbound contract.
- **Three-layer check (bonus)**: PR description derivation was **mechanical with light editing** — the actor's handoff PR suggestion supplied title/what/why/test-steps; the reviewer's edits were de-jargoning ("#48 convention" spelled out) and tightening. Nothing freshly authored. The three-layer split (human PR description / agent handoff / role recipe) held under test.
- **Decisions made**:
  - Reviewer mechanism = Agent-tool subagent (cold context, slice-2 precedent) over claude -p (auth unverified since outage) or a manual fresh session: (decided by: engineer)
  - review.md kept minimal — tribal format contracts deliberately excluded so Q3 measures their absence instead of pre-filling it: (decided by: claude)
  - Reviewer allowed `gh issue view` (durable public state, matches the event-driven reference architecture) but barred from STATE.md and the spike dir: (decided by: claude)
  - F1 disposition: **re-delegate the fix headless as a reviewer-triggered re-entry run** (5th mechanism event — the third route shape from the reference architecture, where the reviewer's finding IS the trigger and durable state carries everything): (decided by: engineer)
- **Re-entry run outcome (boundary action, #47 scope)**: fresh cold actor, envelope = `delegation-prompt-47-reentry.md` (tracked file this time — directly closes Q3 gap C, the envelope is now durable and reviewer-visible). Returned `DONE: 030836f` + `56856d5`; shepherd-verified: diff touches exactly init.md + actor journal, section present in both the Step-3 scaffold template (init.md:121) and the upgrade checklist (init.md:239), fresh trellis stamp pre-commit, baseline 19/4 unchanged, ACTIVE cleaned up, journal Slice 2 entry with literal contracts intact and four `(re-entry)` autonomous decisions. F1 resolved on the #47 branch; the reviewer's request-changes is addressed. Re-entry observation: the actor refused to amend (would orphan the journal's recorded hash) and made a second hash-recording commit — under journal-before-commit ordering, the Commit field is only ever recordable after the fact; cycle 1 should decide whether the fix-up commit is the sanctioned pattern.
- **Acceptance criteria**:
  - [x] Reviewer session genuinely cold — fresh context; inputs only review.md + artifact chain + originating issue; exclusions held (sources log verified)
  - [x] Q3: documentation-gap rubric scored item-by-item — above
  - [x] Q4: role-hypothesis verdict with outside-state inventory — above
  - [x] Q5: one-handoff-vs-two verdict addressing the structured-vs-prose tension — above
  - [x] Three-layer check: PR description drafted from the handoff, derivation noted (mechanical + light editing)
- **Engineer feedback incorporated**: Free-climb slice — mechanism chosen at slice start; boundary review pending.
- **Learnings**:
  - Claude → Engineer: The reviewer leg's value concentrated exactly where the actor's confidence was lowest *and* where the pipeline had silently narrowed scope — the missing-context log instrument (review.md section 3) is what surfaced the envelope-visibility gap; without forcing contemporaneous logging, the reviewer would likely have reported only F1–F4 and the Q3/Q4 evidence would not exist.
  - Claude → Engineer: Three of the four unpredicted gaps (A–C) point at the same cycle-1 design move: the eligibility gate's output (verdict + constraints + allowlist + validation baseline) must be a durable artifact the reviewer can read — slice 1's "verdict plus constraints" learning, now with reviewer-side evidence.
  - Engineer → Claude: At the F1 disposition the engineer chose the re-delegation over the faster shepherd-fix — evidence over speed, consistent with the spike's deliverable being answers rather than the #47 code. That choice produced the 5th-mechanism event and the live event-driven re-entry data point that Q2's consolidation leans on.

### Slice 4: Calibration probe + findings consolidation (Q6) — Complete
- **Started**: 2026-06-12
- **Commit**: 25129c9 (pre-rebase 9e9541a)
- **Route**: interactive (shepherd session); the *probe* ran headless (Agent-tool subagent, cold parse) <!-- throwaway scaffold (spike Q2/Q6) -->
- **Actor**: Rob + Claude (shepherd); probe actor: claude (cold subagent, agentId a7714d1c543a3b559) <!-- throwaway scaffold (spike Q2/Q6) -->
- **Approach taken**: Ran the Q6 parse test as a fresh subagent given ONLY the three scaffolded homes (both NAVIGATION.md journals + PROJECT-MAP.md) — parsing as evolve would, not inspecting — with every extraction classified *mechanical / inferable / absent*. Then consolidated all six roadmap answers with evidence pointers and tagged each scaffold keep/discard. Also closed two journal-accuracy gaps the probe itself surfaced: slice 2's stale Actor field (said `claude -p`; the 4th mechanism delivered — original value preserved in the field comment as evidence) and slice 2's under-recorded mechanism deviation (now recorded, with the SPEC.md decision-2 addendum the deviation-closure contract requires).
- **Deviations from spec**: None.
- **Validation**: pass — vine-verification agent (slice mode); all 3 slice ACs verified; structural contracts pass; no script changes (run-tests baseline unchanged).
- **Q6 probe result (parse-as-evolve-would, full extraction tables in the probe record)**: **Verdict: calibration CAN build its (slice, gear, route, actor, outcome) + (scope, route, outcome) tables from the scaffolded homes without a new artifact — except gear, which fails outright.** Mechanical everywhere: `**Route**:` field (5/5 entries), `**Actor**:` field (5/5), `**Commit**:` field (5/5), completion-status heading literal (5/5), `(decided by:)` tags (10/10 shepherd decisions), the PROJECT-MAP Route table (4 rows × 4 columns retrieval, clean leading tokens: `interactive`/`headless`/`headless re-entry`, `PERMIT`, `delivered`). Failures and degradations, with smallest fixes:
  1. **slice.gear — fails (0/5 mechanical)**: no field exists; the value is a prose fragment ("Free-climb slice") inside Engineer-feedback in 2 entries, absent in 2, cross-file in 1; absent at scope level. Fix: add a `- **Gear**:` line to the slice-entry schema (headless entries record the prompt-collapsed value).
  2. **headless validation verdicts — inference-only (0/2)**: the actor journal's Validation fields open with raw command output, no pass/fail rollup token (shepherd journal: 3/3 lead with `pass`). Fix: one-sentence contract — Validation's first token is `pass`/`fail`, evidence after.
  3. **multi-commit outcomes overflow the Commit field**: the re-entry's hash-recording commit `56856d5` is absent from the actor journal entirely (only the shepherd journal + Route table carry it). Fix: define Commit as `+`-separated hashes; the fix-up commit appends itself.
  4. **headless decision attribution — structurally different, workable**: section-membership (`Decisions Taken Autonomously` = actor-decided) is greppable and confidence tags are mechanical (12/12), but slice association rides on the actor's invented `(re-entry)` prefix. Fix: sanction section-scoped attribution; require a `(slice N)` prefix.
  5. **route mechanism — unstructured parenthetical, went stale-wrong once** (slice 2 Actor field said `claude -p` post-divergence; mechanically extracted value ≠ truth). Fix: controlled Route vocabulary + a labeled `mechanism:` token; require field correction (not just prose) when the mechanism diverges mid-slice.
  6. **scope gate-record — pointer into prose; "inherited PERMIT" unresolvable mechanically**. Fix: the gate verdict+constraints get a durable file home and the cell becomes a path — the one place the spike's evidence justifies a new artifact (converges with Q3 gaps A–C).
- **The six answers, consolidated for evolve** (each with its evidence home):
  - **Q1 — eligibility predicate mechanically checkable?** *Checkable in principle, not with today's fields*: 0/4 legs fully mechanical (Slice 1 per-leg verdicts — validation contract and structured ACs input-absent; blast-radius has a mechanical grep core; independence a mechanical sub-input). Three run-discovered amendments: the gate's output must be **verdict + constraints** (Slice 1 learning — the branch-point constraint); verdicts **decay** — independence went stale within hours (Slice 2 obs 6); occurrence-grep blast-radius misses **requirement-implied files** — F1's root cause (Slice 3). → cycle 1 #54.
  - **Q2 — headless entry + commit without tripping/bypassing journal-check/ACTIVE?** *Yes, three times* (actor `bfe5458`, reviewer read-only, re-entry `030836f`+`56856d5`): ACTIVE written/cleaned correctly in every completed run; journal-check never blocked and never visibly fired (fired-and-passed vs. didn't-fire indistinguishable — live evidence for the hook-scoping debt, Slice 2 obs + Environment Observations); trellis-gate satisfied via fresh stamps in the actor's checkout. Platform boundaries found: nested Agent tool unavailable to subagents (vine-verification can't run inside a headless actor — it self-checks instead); `CLAUDE_PROJECT_DIR` unset in subagents; actor auth must be verified server-side (`claude setup-token` is the sanctioned headless path); actor permissions are a provisioning-time human authority no shepherd can broker (obs 7). The headline: **mechanism portability** — the envelope was rewritten three times across four mechanisms plus a re-entry; the contract core (gate verdict+constraints, allowlist, ACs, validation contract, journal schema, autonomy split, handoff shape) survived every swap unmodified. "VINE is the map, not the mechanism" is the Q2 answer. → cycle 1 #53/#54/#79.
  - **Q3 — reviewer orients from artifacts alone?** *Yes — load-bearing review with a true major finding (F1), validation independently re-run.* The predicted artifact-format gaps scored 0 hard blocks of 8 (Slice 3 item-by-item); the four gaps that bit are delegation-route-shaped: overlay adoption-scope, validation-baseline home, envelope visibility, section-ordering contract. Highest-value chunk is a **durable gate-record + envelope**, not STATE.md's schema (CONTEXT.md's guess overturned). → #53.
  - **Q4 — role = overlay stack + entry point + handoff contracts?** *Holds, with the gate record/envelope promoted into the handoff contracts.* A ~50-line minimal review.md was a sufficient entry point; the re-entry run reinforced it (trigger event + durable state sufficed for a cold fix). Honesty caveat: subagents auto-load CLAUDE.md, and it answered one load-bearing question (adoption scope). → #53/#54.
  - **Q5 — one handoff artifact for outbound + inbound?** *One per-feature handoff suffices; the "second thing" is the durable role recipe, not a second artifact.* The confidence-tagged Decisions Taken Autonomously block served both directions — the cold reviewer independently engaged exactly the two medium-confidence entries. PR description derivation: mechanical with light editing (three-layer split held). Binding inbound requirement: the recipe must mandate reading the originating scope directly — that mandate, not the handoff, caught F1. → #53.
  - **Q6 — journaling homes sufficient for evolve?** *Yes-except-gear* (probe result above): four small schema fixes + one durable gate-record home; no new calibration artifact needed. → cycle 1 schema work.
- **Scaffold keep/discard tags** (spike AC):
  - `**Route**:`/`**Actor**:` per-slice fields — **KEEP** (5/5 mechanical both); cycle 1 adds controlled vocabulary, a `mechanism:` token, and a sibling `**Gear**:` field.
  - PROJECT-MAP Route table — **KEEP** (best-performing scaffold: 4×4 mechanical retrieval; represented multi-actor scopes the Progress table can't); Gate-record cell becomes a path to the durable gate record.
  - `.vine/context/review.md` — **KEEP** (the Q4 entry point, worked first try); promote from throwaway, adding "read the durable gate record" to its orientation order.
  - `Decisions Taken Autonomously` block — **KEEP** (outbound record + inbound triage simultaneously); sanction section-scoped attribution with a required `(slice N)` prefix.
  - Delegation-prompt files — **KEEP the durable-envelope pattern** (v4 closed gap C); v1–v3 remain archived evidence in this feature dir. Cycle 1 merges gate record + envelope into one durable artifact (#54/#53).
  - Throwaway HTML markers themselves — **DISCARD at adoption** (replaced by real schema entries in STATE.md).
- **Decisions made**:
  - Q6 probe run as a cold subagent parse over only the three homes, not in-context inspection — honesty of the parse test over convenience: (decided by: claude)
  - Slice 2's stale Actor field corrected with the original value preserved in the field comment — accuracy and evidence both kept: (decided by: claude)
  - Slice 2's mechanism divergence promoted from Decisions-made prose to the Deviations field + SPEC addendum, per the intent-over-letter AC standard: (decided by: claude)
- **Acceptance criteria**:
  - [x] Q6 verdict from actually parsing as evolve would (cold subagent, extraction tables with per-cell mechanical/inferable/absent classification) — not inspection
  - [x] All six answers consolidated with pointers to backing observations, ready for evolve
  - [x] Each scaffold tagged keep/discard for cycle 1
- **Engineer feedback incorporated**: Free-climb slice — boundary review pending.
- **Learnings**:
  - Claude → Engineer: The parse probe earned its cost in one finding class nothing else produced: *mechanically extracted value ≠ truth* (the stale `claude -p` Actor field). Inspection would have read the prose correction and never noticed the field lied — only a parser that refuses to read prose catches schema rot.
  - Claude → Engineer: Every Q3–Q6 failure mode converged on the same cycle-1 artifact: a durable gate record (verdict + constraints + allowlist + validation baseline) that the envelope embeds, the reviewer reads, and the Route table points at. The spike's findings are six answers but arguably one design move.

### Remaining Work
- **Incomplete slices**: All slices complete (4 of 4; commits f8600fd, b20a1fe, d2c4c88, 25129c9 — rebased onto main `287fd02` at navigate close, pre-rebase hashes preserved per entry).
- **Blockers encountered**: None outstanding. Three platform failures during slice 2 (headless auth, remote provisioning, scheduled-task permission deadlock) were resolved by mechanism swaps and are recorded as Q2 evidence, not open blockers.
- **Handoff context**: Evolve's job is compiling the six consolidated answers (Slice 4 entry) into EVOLUTION.md and turning the convergent finding — the durable gate record — into cycle-1 issue input for #54 (gate) and #53 (handoff). The keep/discard tags (Slice 4) are the scaffold dispositions; review.md's promotion from throwaway is the one durable-mechanism call evolve must make against the "no durable mechanism beyond what findings justify" AC. The delegated #47 branch (`feature/askuser-constraints` in the headless-47 worktree, commits bfe5458 + 030836f + 56856d5) is reviewed (request-changes resolved by the re-entry run) and awaits the shepherd's PR call — the reviewer's derived PR description is in its report (Slice 3) and the actor journal's PR suggestion is amended for the init refresh. Tech debt observed-not-fixed per spec: journal-check mtime (never visibly fired across three headless commits — firing indistinguishable from outcomes), ACTIVE lifecycle (clean in all completed runs; the deadlocked scheduled-task session never reached it — feeds #79), run-tests fixture baseline (fixed on main by #89; this branch picked the fix up in the navigate-close rebase — 24/24 green post-rebase — while headless-47 still predates it, so the actor journal's 19/4 records stay checkout-accurate). Retro items persisted here: (1) consider a CLAUDE.md/shared.md note that agent reports are findings-trustworthy, diagnosis-unverified — three subagent reports this feature were accurate in findings while one inverted a root cause; (2) candidate skill/automation: the shepherd's post-run verification sweep (diff-vs-allowlist, stamp freshness, journal-literal checks) repeated near-identically after every delegated run and is mechanizable; (3) the `(decided by:)` convention plus confidence tags carried real calibration weight — schema work in cycle 1 should formalize both, per the Q6 probe's fix list.
